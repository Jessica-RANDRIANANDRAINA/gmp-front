

const InterContract = () => {
  return (
      <div className="grid place-items-center h-[50vh] font-bold text-2xl">
        <div className="flex flex-col justify-center items-center">
          <span>Cette page (intercontrat) est en construction 😊</span>
        </div>
      </div>
  );
};

export default InterContract;
